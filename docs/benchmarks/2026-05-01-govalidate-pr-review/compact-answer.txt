## PR Diff Review (compact)

### Changed areas
- **Admin pipeline dashboard** — Major rewrite. New extracted state module (`page-state.ts` + tests), refactored detail page (1.3k lines), new dashboard page, and 4 new admin components (`PipelineCostBreakdownPanel`, `PipelineFailuresList`, `PipelineRunsTable`, `PipelineSummaryCards`) with supporting hooks (`usePipelineAdmin`, `usePipelineRunDetail`), API service, and types.
- **Idea details UX** — New `RecommendationBadge` (178 lines) wired into `IdeaHero`, `LetsBuildTab` (heavy edits ~150 lines), `RunwayTab`, plus tweaks to `IdeaMetricsReview` and `RedesignedIdeaDetailsPage`.
- **Layout** — Small touches in `src/components/layout.tsx` (3 small ranges; likely nav entry for admin pipeline).

### Top risks
1. **`PipelineRunDetailPage()`** — high blast radius (601 dependents, 272 communities); bridge + god-node hotspot. Touches `SidePanel`, live cost merging, hydrated status resolution.
2. **`IdeaMetricsReview()`** — 605 dependents, 282 communities; small line changes but it's a heavily-referenced component.
3. **`PipelineDashboardPage()`** — bridge hotspot, 601 dependents.
4. **State merge correctness** — `mergeRunSections()`, `mergeLiveCost()`, `resolveOverviewTotals()`, `shouldRefetchRunDetail()`, `isTerminalRunSnapshot()` are new pure functions controlling refetch + display totals. Subtle bugs here (terminal-state detection, totals source precedence) can cause stuck spinners or stale costs.
5. **`LetsBuildTab`** — wide-ranging edits (15+ ranges) — regression risk in the build/copy/area-selection flow.

### Supporting files to inspect
- `src/app/admin/pipeline/[ideaId]/page-state.ts` — source of truth for all merge/totals/terminal logic
- `src/features/admin/services/pipeline-api.ts` + `types/pipeline.types.ts` — API contract surface
- `src/features/idea-details/hooks/pipeline-status-sync.ts` — adjacent sync logic that may interact with merge changes
- `src/features/ideas/components/PipelineDAG.tsx` + `IdeaProgressV2.tsx` — consumers of the same `PipelineStatus`/`ProgressBroadcast` types
- `src/features/idea-details/components/SmartContent.tsx` — used by new `RecommendationBadge`

### Likely tests to run
- `src/app/admin/pipeline/[ideaId]/page-state.test.ts` (new, 430 lines — the safety net)
- `src/features/idea-details/hooks/pipeline-status-sync.test.ts`
- Type check: `npm run type-check`
- Manual: load `/admin/pipeline`, then a run detail page; verify live socket updates merge cost + section progress; trigger a terminal state to confirm refetch stops.
- Manual: idea details — `RecommendationBadge` render in `IdeaHero`/`RunwayTab`/`LetsBuildTab`; build flow end-to-end.

### Structural hotspots to watch
- **`SidePanel()`** in `page.tsx:645` — bridge connecting cost, sections, status communities. Prop surface likely shifted.
- **`PipelineRunDetailPage()`** — god-node; verify socket subscription cleanup and that `subscribeTo` from `useSocket` is paired with unsubscribe.
- **Cross-community drift** — 50 cross-community changes; the new `pipeline-api.ts` + types create a new admin API community — confirm no duplicate type definitions vs `@/shared/lib/api`.
- **Terminal/refetch loop** — `shouldRefetchRunDetail()` + `isTerminalRunSnapshot()` together govern polling; an off-by-one on status enum (recall: `IdeaStatus` is uppercase) would either hammer the API or freeze the UI.

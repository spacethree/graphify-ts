# graphify-ts current status and roadmap

This document keeps the implementation status, scope notes, limitations, and next-step planning details that were moved out of the top-level `README.md` so the README can stay end-user focused.

## Current status

Implemented in this repository:

- project scaffold with strict TypeScript + Vitest
- extraction schema contracts and validation
- additive schema-v2 extraction metadata contracts for `schema_version`, layered `base` / `semantic` / `media` tagging, provenance validation on nodes / edges / hyperedges, and fixture-backed regression coverage for legacy-v1 compatibility
- schema normalization helpers that upgrade legacy extraction payloads immutably with default schema-version / layer / provenance metadata and apply those defaults during graph build
- cache utilities with atomic writes
- corpus/file detection with `.graphifyignore`, hidden-file skipping, symlink controls, incremental manifest helpers, and explicit `graphify-out/memory/` note inclusion without re-ingesting generated graph artifacts
- graph assembly from extraction JSON
- shared extractor helpers now split under `src/pipeline/extract/` to keep the main pipeline entry point smaller and more modular
- extraction MVP for:
  - Python, Go, Java, and Ruby via portable `web-tree-sitter` + `@vscode/tree-sitter-wasm` parsing for owner-aware type/method extraction, imports, async Python function support, rationale attachment, and common call-graph edges, including Ruby command-style call resolution plus singleton/constant-receiver dispatch for common fixture-backed cases
  - Lua and `.toc` addon manifests via lightweight source/metadata scanners
  - Elixir and Julia via lightweight end-delimited source scanners
  - PowerShell and Objective-C via lightweight platform-aware source scanners
  - JavaScript / TypeScript via the TypeScript compiler API, including static imports, re-exports, `import = require(...)`, CommonJS `require(...)`, dynamic `import()`, and direct call-graph coverage
  - remaining brace-style languages (currently including Kotlin, Scala, C#, Rust, Swift, PHP, Zig, and C/C++-style signatures) via a portable regex-based structural extractor with common inheritance/conformance support, including multiline Scala/Kotlin/Swift signature coverage from the fixture-backed parity cases
  - Markdown / text / RST documents via deterministic section parsing, frontmatter metadata lifting (`title`, `source_url`, `captured_at`, `author`, `contributor`, `source_nodes`, etc.), explicit raw ingest-provenance emission for registry-driven text captures, normalization-time ingest-provenance fallback/dedupe for backward compatibility, source-node reference edges, local-reference detection, DOI/arXiv/LaTeX-citation extraction, numbered-citation resolution, and bibliography-reference nodes enriched with local reference metadata plus explicit bibliography `source_url` lifting when plain external URLs are present
  - DOCX documents via synchronous zip/XML title/author/subject/description lifting, heading extraction, local citation detection, and basic numbered-reference parsing inside detected references sections
  - XLSX spreadsheets via synchronous zip/XML workbook metadata, sheet-name extraction, and shared-string citation detection
  - text-like paper corpora plus heuristic PDF title/author/subject extraction, `Tj`/`TJ` text recovery, inline citation detection, and basic numbered-reference extraction when recovered text includes local bibliography entries
  - image assets as first-class graph nodes
- sidecar-backed non-code extraction now fans out explicit raw ingest provenance across PDF/image/DOCX/XLSX nodes and edges (including file-only DOCX fallback when hidden sidecar metadata is present), while normalization remains the backward-compatible fallback/dedupe path for older payloads
- cache-backed multi-file extraction for the implemented corpus types, including extractor-cache version invalidation when raw extraction metadata contracts change so stale pre-metadata payloads do not leak back through `extract()`
- bridge-aware community clustering and cohesion scoring
- graph analysis helpers for god nodes, surprising connections, semantic anomaly detection (bridge nodes, low-cohesion communities, cross-boundary edges), suggested questions, file/category heuristics, and graph diffs
- graph query/runtime helpers for traversal, node/community lookup, stats, shortest-path summaries, graph-diff summaries, semantic anomaly summaries, ranked query results, and query scoping by community/file type
- export surfaces for JSON (including additive `community_labels` and `semantic_anomalies` metadata), Cypher, GraphML, a richer interactive HTML explorer with confidence-aware inferred-edge styling plus selected-node metadata/source URL surfacing, automatic overview-first large-graph fallback pages, deterministic semantic community names across human-facing outputs, and Obsidian vault output
- markdown graph report generation with semantic-anomaly and suggested-question sections
- wiki/article export for communities and god nodes
- benchmark helpers for token-reduction reporting
- a built TypeScript CLI for top-level graph generation plus `watch`, `serve`, `query`, `diff`, `path`, `explain`, `add`, `install`, `save-result`, `benchmark`, and `hook`, including optional `generate --update`, `--directed`, `--wiki`, `--obsidian`, `--svg`, `--graphml`, `--neo4j`, direct `--neo4j-push` graph export/push support, and broader `serve` transport selection via `--transport`, `--http`, `--stdio`, and `--mcp`
- direct Neo4j push support via the official `neo4j-driver`, with credentials resolved from `--neo4j-user` / `--neo4j-password` / `--neo4j-database` flags or `NEO4J_*` values in the local environment / `.env` (copy `.env.example` to `.env` as a starting point)
- the `serve` command can now run as either a lightweight HTTP runtime or a stdio runtime via `--transport http|stdio`, `--http`, `--stdio`, or `--mcp`, including an MCP-compatible `initialize`, `prompts/list`, `prompts/get`, `resources/list`, `resources/read`, `tools/list`, `tools/call`, `completion/complete`, and `logging/setLevel` surface with snake_case tool arguments, prompt completions, log notifications, ranked query support, graph-diff summaries against a baseline graph, semantic-anomaly summaries, community/file-type query scoping, graph-aware prompt descriptions, a `graph_community_summary_prompt` seeded from actual community labels / god nodes / suggested questions, a `semantic_anomalies` tool plus direct `anomalies` method, an HTTP `/anomalies` endpoint, and freshness/version metadata for served artifacts via MCP resource annotations plus HTTP graph-version headers
- local platform/project configuration commands for `claude`, `gemini`, `cursor`, `codex`, `opencode`, `aider`, `copilot`, `claw`, `droid`, `trae`, and `trae-cn`
- self-contained installer skill templates managed in the TypeScript package, without any Python runtime or nested reference checkout dependency
- git hook install / uninstall / status helpers
- npm distribution with the global `graphify-ts` executable, a prepack build, and a publish-time file allowlist
- open-source contributor / maintainer scaffolding via `CONTRIBUTING.md`, `SECURITY.md`, GitHub issue forms, a pull request template, `CODEOWNERS`, a CI workflow, and a maintainer-facing GitHub settings checklist
- release-tag handling via a `v*`-triggered GitHub release workflow plus maintainer docs for protected tags and version/changelog alignment
- portable watch helpers for extension filtering and automatic rebuilds across supported code, document, paper, image, and office corpora, with incremental refresh reuse when graph artifacts already exist
- a lightweight Node HTTP runtime that serves `graph.html`, `graph.json`, `GRAPH_REPORT.md`, and graph query endpoints from the CLI
- security utilities for graph path validation, URL validation, safe fetch, and label sanitization
- ingest helpers for URL classification, safe webpage/binary capture, and query-result memory files
- a builtin capability registry foundation with normalized extension lookup, source-classification-aware extension disambiguation, duplicate-id / same-classification duplicate-extension protection, and fixture-backed coverage for supported extractor families
- registry-driven single-file extraction dispatch via `src/pipeline/extract/dispatch.ts`, including cache-aware handler routing and correct markdown/text paper-vs-document resolution without re-expanding the old `extractSingleFile()` extension chain
- registry-driven multi-file extraction aggregation via `src/pipeline/extract/combine.ts`, including stable fragment merging plus `source_nodes` → `references` resolution across extracted nodes and optional context nodes without re-expanding that logic back into the main façade
- registry-driven Python cross-file linking via `src/pipeline/extract/cross-file.ts`, including preserved inferred `inherits` / `uses` resolution for fixture-backed `from ... import ...` class relationships, optional context-node lookups, and graceful skipping of unreadable Python files without keeping that linker inline inside the façade
- registry-driven ingest dispatch via `src/infrastructure/ingest/dispatch.ts`, including builtin ingest capability resolution by URL kind plus stable fallback routing of current GitHub/YouTube-style URLs through the existing webpage capture path
- end-to-end pipeline coverage across detect → extract → build → cluster → analyze → report → export
- local test fixtures owned by this repository under `tests/fixtures/`, so the test suite does not depend on a separate Python checkout

## Future work beyond the completed roadmap slice

The roadmap slice tracked for this repository is implemented. Remaining follow-ups are broader stretch goals rather than blockers for the current TypeScript port:

A detailed roadmap for closing the biggest upstream gaps and pushing beyond parity now lives in [`2026-04-12-upstream-parity-and-beyond.md`](./2026-04-12-upstream-parity-and-beyond.md).

The next concrete Phase 1 slices are now shipped: additive schema-v2 validation, a builtin capability-registry foundation, immutable schema normalization for legacy payload upgrades inside the graph-build path, registry-driven single-file extraction dispatch, registry-driven multi-file extraction aggregation, registry-driven Python cross-file linking, registry-driven ingest dispatch, explicit top-level `schema_version` preservation through `build()`, `graph.json` export, graph reload, and `generate --update`, helper-created raw extraction output that now emits explicit base-layer/provenance metadata before normalization, cache invalidation for stale pre-metadata extractor payloads, explicit raw ingest-provenance emission for registry-driven text captures while keeping captured files backward-compatible on disk, normalization-time ingest-provenance fallback/dedupe for backward compatibility, hidden binary ingest sidecars for direct PDF/image captures so extracted file nodes can lift flat `source_url` / `captured_at` / `contributor` metadata without changing the returned asset path, sidecar-aware extractor cache hashing so provenance updates invalidate cleanly, explicit raw ingest-provenance fan-out across sidecar-backed PDF/image/DOCX/XLSX extraction (including corrupted-DOCX file-only fallback), and focused regression coverage for specialized image/PDF/file-only extraction paths, representative code-extractor metadata parity across JavaScript/TypeScript, Go, Java, Ruby, Lua, Elixir, Julia, PowerShell, Objective-C, and generic fallback languages (Rust/Kotlin/Swift/C-family), Python rationale nodes/edges, markdown/PDF/DOCX citation-linkage parity (including LaTeX multi-key splitting, arXiv format variants, reference-to-citation links, bibliography-section detection, and inline numeric forward-reference resolution), local `references` edges across markdown/document/paper/support extraction plus combine-resolved `source_nodes`, DOCX heading/section nodes plus `contains` edges, DOCX citation nodes plus `cites` edges, DOCX reference-section nodes plus `contains` / reference-`cites` edges, PDF reference-section nodes plus `contains` / reference-`cites` edges, XLSX sheet nodes plus workbook-`contains` edges, XLSX citation nodes plus `cites` edges, flat tweet/arXiv ingest output, and URL-type-specific tweet/X, GitHub, YouTube, arXiv, PDF, image, and generic webpage ingest provenance selection across raw extraction plus normalization fallback.

- widen portable tree-sitter coverage beyond the current Python/Go/Java/Ruby AST-backed slice
- deepen bibliography/metadata resolution beyond the current frontmatter/query-memory lift, local reference metadata parsing, and deterministic citation/link resolution
- broaden MCP/stdIO protocol breadth beyond today’s lightweight prompt/resource/tool runtime, subscriptions, prompt completions, and logging controls

## Known limitations

- Python extraction now runs through the same portable WASM tree-sitter path as Go and Java. The current AST-backed slice covers the fixture-backed structural, async-function, rationale, and common cross-file `from ... import ...` parity cases already under test, while deeper language-specific edge cases still remain lighter than a dedicated full-reference parser stack.
- Ruby extraction now runs through the same portable WASM tree-sitter path as Python, Go, and Java. The current AST-backed slice covers common `module` / `class` / `def` structure, `require` / `require_relative` imports, inheritance, direct calls, command-style no-paren calls, and common singleton/constant-receiver dispatch already covered by fixture-backed tests, while metaprogramming-heavy Ruby edge cases still remain lighter than a dedicated full-reference parser stack.
- Lua, `.toc`, Elixir, Julia, PowerShell, and Objective-C extraction now cover common module/class/function or metadata structure plus straightforward imports, references, and call edges, but they remain lightweight scanners aimed at common source patterns rather than full language parsers.
- JavaScript / TypeScript extraction now covers classes, interfaces, heritage clauses (`extends` / `implements`), methods, nested function declarations/closures, top-level and class-field arrow/function-expression bindings, static imports, re-exports, `import = require(...)`, CommonJS `require(...)`, dynamic `import()` usage, and direct call relationships. More advanced decorator-heavy metadata and some uncommon AST edge cases are still lighter than a fuller parser-backed implementation.
- Go and Java extraction now run through the same portable WASM tree-sitter path used for Python and Ruby. The current AST-backed slice focuses on common type, interface, record, method, import, and call-graph patterns already covered by tests; deeper language-specific edge cases still need more parity work.
- The remaining portable multi-language extractor is intentionally heuristic. It now emits useful structure for several brace-style languages, including owner-aware method extraction for common `impl` / `extension` blocks, common inheritance/conformance clauses, Kotlin / Scala expression-bodied definitions, Swift `func ... -> ...` signatures, Zig `@import`/`struct`/`fn` patterns, and qualified `Type::method` definitions, but it is still regex-based rather than AST-backed, so deeply nested signatures, macros, or language-specific edge cases can still be missed.
- Document and text-like paper extraction is still deterministic rather than generative, but it now models headings, containment, markdown links, lifted frontmatter/query-memory metadata, explicit raw ingest provenance for registry-driven text captures with normalization-time fallback/dedupe for backward compatibility, source-node references, local filename mentions, DOI/arXiv identifiers (including old-style, versioned, and URL-prefixed arXiv forms), LaTeX-style `\cite{...}` keys with multi-key splitting, numbered bibliography references, reference-to-citation links for bibliography entries, inline numeric-citation links back to local bibliography nodes, and explicit bibliography `source_url` lifting when numbered reference entries include plain external URLs. It also lifts basic local reference metadata such as year/title/DOI/arXiv when those are present, but it does not yet resolve external paper metadata.
- DOCX documents now get a lightweight synchronous zip/XML pass for title/author/subject/description lifting, heading structure, local filename mention references, inline citation detection, and basic numbered-reference parsing when a references section is detected.
- XLSX spreadsheets now land as document nodes with workbook metadata, sheet-name structure, workbook-to-sheet containment, and shared-string citation detection, but they still do not extract full cell-level semantics or formulas.
- PDF papers now get a lightweight heuristic pass for metadata title/author/subject, `Tj`/`TJ` text extraction, local filename mention references, inline citation detection, and basic numbered-reference parsing when local bibliography lines are recoverable from heuristic text operations, but binary paper/document formats are still not parsed as deeply as the richer text/document formats.
- The HTML visualization export now supports a selected-node details panel, clickable neighbor navigation, searchable node matches, community focus controls, URL-hash deep links, confidence-aware inferred-edge styling, lifted metadata/source URL display (validated before links are made clickable), and deterministic semantic community names instead of plain `Community N` placeholders. Large corpora now auto-switch to an overview-first HTML mode that links into focused per-community pages so the browser does not have to render the entire graph at once. It is still lighter than the richer Python reference UI overall, but it no longer lacks the basic inspection/navigation surface.
- Directed graph mode is now supported via `generate --directed`: exported JSON persists `directed: true`, runtime query/path traversal follows outgoing edges, and GraphML emits `edgedefault="directed"`. Community detection still intentionally treats the graph as undirected connectivity, matching the Python reference's clustering behavior.
- Direct Neo4j push is now implemented for the CLI via `--neo4j-push`, but it assumes a reachable Neo4j instance, a supported `bolt://` / `neo4j://` style URI, and credentials supplied through flags or `NEO4J_*` values in the shell / local `.env` file.
- A direct Node native `tree-sitter` install failed in this environment under Node 25, so the TS port currently uses portable WASM tree-sitter for Python/Go/Java/Ruby and keeps non-AST fallbacks elsewhere instead of relying on native parser addons.
- The installer now uses bundled TypeScript-managed skill templates by default. Package-local markdown assets can still override those templates when present, which keeps customization possible without introducing a Python dependency.
- The watch layer now rebuilds supported code, document, paper, image, and office-document changes automatically, wakes promptly on filesystem events, and reuses incremental generation when graph artifacts already exist.
- `generate --update` now reuses the existing graph as incremental context, re-extracts only changed/new supported files, drops deleted-source graph records, preserves top-level `schema_version` while rebuilding from retained graph context, and ignores generated `graphify-out/` artifacts while still allowing saved memory notes under `graphify-out/memory/` to flow back into the corpus.
- Schema-v2 validation, legacy normalization, `graph.json` schema-version export, graph reload, incremental update preservation, helper-created raw extraction metadata, and ingest-provenance handling are now additive and backward-compatible, and both single-file extraction dispatch and ingest dispatch now route through the capability registry. Focused regressions now lock in explicit `layer` / `provenance` metadata for specialized image/PDF/file-only extraction paths, representative code-extractor families across AST-backed, scanner, and generic fallback handlers, Python rationale nodes/edges, markdown/PDF/DOCX citation-linkage paths (including LaTeX multi-key splitting, arXiv format variants, bibliography-entry citation links, bibliography-section detection, and inline numeric forward-reference resolution), local `references` edges across markdown/document/paper/support extraction plus combine-resolved `source_nodes`, DOCX heading/section nodes plus `contains` edges, DOCX citation nodes plus `cites` edges, DOCX reference-section nodes plus `contains` / reference-`cites` edges, PDF reference-section nodes plus `contains` / reference-`cites` edges, XLSX sheet nodes plus workbook-`contains` edges, XLSX citation nodes plus `cites` edges, flat tweet/arXiv ingest output, hidden binary PDF/image ingest sidecars plus extraction/cache invalidation coverage, explicit raw ingest provenance across registry-driven text captures and sidecar-backed PDF/image/DOCX/XLSX extraction, and URL-type-specific tweet/X, GitHub, YouTube, arXiv, PDF, image, and generic webpage ingest provenance selection with normalization fallback/dedupe for backward compatibility; future extractor or ingest families should preserve the same explicit raw metadata-emission discipline.
- The new `serve` command is a lightweight HTTP runtime over generated artifacts and query helpers. It is useful today, but it is not yet a full MCP/stdIO replacement.
- The new `serve --transport http|stdio` surface (plus `--http`, `--stdio`, and `--mcp`) now supports an MCP-compatible handshake plus `prompts/list` / `prompts/get`, `resources/list` / `resources/read`, `tools/list` / `tools/call`, `completion/complete`, `logging/setLevel`, snake_case tool arguments, graph-aware prompt descriptions, a community-summary prompt, graph-diff reporting against a baseline `graph.json`, semantic-anomaly reporting from stored graph artifacts, freshness/version metadata on both HTTP and MCP resource responses, and `god_nodes`, alongside direct JSON-line query methods and JSON-RPC log notifications. It is still a lightweight runtime rather than a full reference MCP implementation.
- Implemented MCP capabilities today: `initialize`, prompt discovery/retrieval, prompt-argument completions, artifact resource discovery/reading, `resources/subscribe`, `resources/unsubscribe`, resource update/list-changed notifications, logging level control, JSON-RPC log notifications, graph-aware query/path/explain prompts seeded from live graph metadata, `graph_community_summary_prompt`, freshness/version annotations on served resources, and graph query tools including `query_graph`, `graph_diff`, `semantic_anomalies`, `get_node`, `get_neighbors`, `shortest_path`, `explain_node`, `graph_stats`, `god_nodes`, and `get_community`. The HTTP runtime now mirrors that graph snapshot state with additive freshness headers (`x-graphify-graph-version`, `x-graphify-graph-modified-ms`, `etag`, `last-modified`) plus `/anomalies` for lightweight anomaly inspection so clients can detect stale artifacts and triage suspicious graph structure without guessing. Not implemented yet: richer reference-server features such as sampling and broader protocol breadth.

## Repository layout

- `src/` — standalone TypeScript implementation
- `tests/fixtures/` — local fixture corpus owned by this repository
- `tests/unit/` — TypeScript tests, including an end-to-end pipeline suite
- `.github/` — issue forms, pull request template, code ownership, and CI workflow
- `.github/workflows/release.yml` — tag-driven GitHub release validation and release creation
- `docs/maintainers/` — maintainer-only repository settings and protection guidance

## Next likely steps

- widen portable tree-sitter coverage beyond the current Python/Go/Java/Ruby slice
- keep explicit layer/provenance emission locked in for any newly added extractor or ingest families beyond the now-covered builtin set
- deepen bibliography/metadata resolution beyond the current frontmatter/query-memory lift, local reference metadata parsing, deterministic bibliography URL lifting, and deterministic citation/link resolution
- add MCP/stdIO runtime parity on top of the new Node serve surface, especially sampling and broader protocol breadth
- keep extending integration-style coverage as broader runtime surfaces land
